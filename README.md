# Just Music — OLED 风格本地音乐播放器

一款基于 PyQt6 开发的 Windows 本地音乐播放器，采用纯黑 OLED 风格界面设计，支持多种音频格式与内嵌/外挂歌词，追求极简视觉与纯粹播放体验。

---

## ✨ 核心特性

- **OLED 纯黑界面**：全 `#000000` 背景，低亮度护眼，专为 OLED 屏幕优化
- **多格式支持**：MP3、FLAC、WAV、M4A、OGG（自动递归扫描文件夹）
- **元数据自动解析**：通过 mutagen 读取标题、艺术家、专辑、年份、内嵌封面与歌词
- **圆角封面展示**：自定义 `RoundedLabel` 组件，带抗锯齿白色圆角边框
- **实时歌词显示**：支持标准 LRC 格式，当前行高亮 + 下一句预览，拖动进度条时歌词同步预览
- **非线性音量控制**：采用平方曲线映射，更符合人耳响度感知
- **Windows 11 原生沉浸标题栏**：调用 DWM API 实现系统级纯黑标题栏与白色文字
- **智能封面回退**：内嵌封面 → 同目录同名 `.jpg` → `cover.jpg`

---

## 🖼️ 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  [侧边栏]              [顶部播放面板]                         │
│  📁 本地音乐           [封面]  歌名 (28px 粗体)              │
│  🔄 扫描文件夹                年份 / 艺术家 — 专辑           │
│  ────────────────                                    [歌词] │
│  歌曲列表                                                    │
│  春日影                    [中间控制条]                     │
│    MyGO!!!!! — 单曲     ◀◀  [播放]  ▶▶  [进度条]  [音量]   │
│  迷星叫                                                        │
│    MyGO!!!!! — 单曲    ──────────────────────────────────    │
│  ...                                                         │
│  ────────────────      [下方赏析区]                         │
│                        歌曲赏析                              │
│                        暂无歌曲赏析。                        │
└─────────────────────────────────────────────────────────────┘
```

- **左侧边栏**：导航 + 可滚动歌曲列表（选中高亮带左侧白色指示条）
- **顶部面板**：80×80 圆角封面、大字号歌名、年份、艺术家信息、右侧双行歌词
- **控制条**：播放/暂停、切歌、可拖动进度条（支持预览）、非线性音量滑块
- **赏析区**：显示 FLAC `description` 标签或自定义文本

---

## 📥 直接下载（无需编译）

如果你不想安装 Python 或自己打包，可以直接下载编译好的版本：

👉 **[前往 Releases 页面下载最新版](https://github.com/Lucasky-blue/Just-MuJic/releases)**

下载 `Just-Music-v*-windows.zip`，解压后双击 `Just Music.exe` 即可运行。

> ⚠️ **单目录模式**：请解压整个压缩包后运行，勿单独移动 `Just Music.exe`，否则依赖会丢失。

---

## 📦 依赖要求

如果你想从源码运行或二次开发，需要安装以下依赖：

```
PyQt6>=6.4
mutagen>=1.46
```

> 若仅需播放 WAV/MP3 而不读取元数据，可跳过 `mutagen` 安装，程序将自动降级运行。

---

## 🚀 从源码运行

### 1. 安装依赖

```bash
pip install PyQt6 mutagen
```

### 2. 运行

```bash
python main.py
```

程序启动后会自动加载默认路径：
```
C:\Users\你的用户名\Music\Just Music
```
若路径不存在，可通过侧边栏 **"🔄 扫描文件夹"** 手动指定音乐目录。

---

## 🎵 文件组织建议

为获得最佳体验，建议按以下方式整理曲库：

```
Just Music/
├── 春日影.mp3
├── 春日影.lrc          ← 外挂歌词（可选）
├── 春日影.jpg          ← 同名封面（可选）
├── 迷星叫.flac         ← FLAC 内嵌封面/歌词优先读取
└── cover.jpg           ← 文件夹通用封面（回退）
```

---

## 🛠️ 技术细节

| 模块 | 说明 |
|------|------|
| `QMediaPlayer` + `QAudioOutput` | Qt6 原生多媒体后端，无需额外解码器 |
| `mutagen` | 音频元数据解析，支持 ID3 / Vorbis Comment / APE |
| `LRCParser` | 正则解析 `[mm:ss.xx]` 时间戳，支持多时间标签单行 |
| `RoundedLabel` | 自定义 `QLabel`，`QPainter` + `QPainterPath` 实现圆角裁切与边框 |
| `set_dark_titlebar` | `ctypes` 调用 `dwmapi.dll`，适配 Windows 11 22H2+ 沉浸模式 |
| 进度预览 | `sliderMoved` 时计算预览毫秒数，实时驱动歌词更新，释放后跳转 |

---

## 📋 自行打包为可执行文件

如果你想自己打包，使用 PyInstaller：

```bash
pyinstaller --onefile --windowed --name "JustMusic" main.py
```

> 若使用 FLAC/OGG 等格式，请确保目标机器已安装相应 DirectShow 滤镜或 Qt 多媒体插件。

---

## 🔮 未来扩展方向

- [ ] **SQLite 曲库索引**：避免每次全量扫描，支持搜索与排序
- [ ] **详情页抽屉**：点击歌曲右侧滑出全屏封面、滚动歌词、专辑信息（仿 QQ 音乐）
- [ ] **全局快捷键**：注册系统热键控制播放/暂停/切歌
- [ ] **频谱可视化**：基于 `QAudioProbe` 或 `QPainter` 绘制实时波形
- [ ] **播放列表管理**：支持队列编辑、循环/随机模式
- [ ] **多主题切换**：在 OLED 黑、深灰、暗蓝等 QSS 主题间切换

---

## 📝 开源协议

MIT License — 自由使用与修改。
